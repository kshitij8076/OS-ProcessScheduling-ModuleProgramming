#include <stdlib.h>
#include <stdio.h>
#include <math.h>
#include <time.h>

#define COUNT_LIMIT (pow(2, 32))

int main(char *args, char *argv[])
{
    // clock_t start_time, end_time;
    // printf("couting dollars\n");
    // start_time = clock();

    for (unsigned long long i = 0; i < COUNT_LIMIT; i++)
    {
        continue;
    }

    // end_time = clock();

    // double elapsed_time = (double)(end_time - start_time) / CLOCKS_PER_SEC;

    // printf("Time taken: %f seconds\n", elapsed_time);
    return 0;
}
